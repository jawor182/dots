#!/bin/sh

if [ "$XDG_SESSION_TYPE" = "x11" ]; then
    color=$(xcolor)
    echo "$color" | xclip -selection clipboard
else
    color=$(grim -g "$(slurp -b 00000000 -p)" -t ppm - | magick - -format '#%[hex:p{0,0}]\n' info:-)
    echo "$color" | wl-copy
fi

notify-send "Picked $color"
