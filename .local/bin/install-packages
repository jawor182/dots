#!/usr/bin/env bash

sudo pacman -Syyuu --needed $(cat "$HOME/.local/share/cave/arch/packages.txt")

if [ ! -d "$HOME/.local/yay" ]; then
    setup-yay
fi

yay -Syu --needed $(cat "$HOME/.local/share/cave/arch/yay-packages.txt")
